class: Hvac

table: hvacs

fields:
  macAddress: String
  type: DeviceType
  flameSensor: FlameSensor
  tempSensor: TempSensor
  ventActuator: VentActuator
  alarm: Alarm
  lastUpdate: DateTime?
indexes:
  hvac_mac_idx:
    fields: macAddress
    unique: true