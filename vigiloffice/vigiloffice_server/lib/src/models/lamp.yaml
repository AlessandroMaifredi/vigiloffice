class: Lamp

table: lamps

fields:
  macAddress: String
  type: DeviceType
  lightSensor: LightSensor
  motionSensor: MotionSensor
  flameSensor: FlameSensor
  rgbLed: RGBLed
  alarm: Alarm
  lastUpdate: DateTime?
indexes:
  lamp_mac_idx:
    fields: macAddress
    unique: true